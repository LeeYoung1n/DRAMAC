<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="com.dramac.mapper.PhotoMapper">


	<insert id="write">
	
		<selectKey keyProperty="bno" order="BEFORE" resultType="int">
			select max(bno)+1 bno
			from drama_photoboard;
		</selectKey>	
	
		insert into drama_photoboard(bno, title, content)
		values (#{bno}, #{title}, #{content})
	</insert>
	
	
	<select id="list" resultType="com.dramac.model.PhotoBoardVO">
		select *
		from (
				select @rownum:=@rownum+1 rownum, drama_photoboard. *
				from drama_photoboard, (select @rownum:=0) as tmp
				<if test = "keyword != null">
				<choose>
					<when test="type == 'T'.toString()">
					where title like concat('%', #{keyword}, '%')
					</when>
					<when test="type == 'C'.toString()">
					where id like concat('%', #{keyword}, '%')
					</when>
					<when test="type == 'W'.toString()">
					where content like concat('%', #{keyword}, '%')
					</when>
					<otherwise>
					where title like concat('%', #{keyword}, '%')
					OR content like concat('%', #{keyword}, '%')
					</otherwise>
				</choose>
				
				</if>
				order by bno desc
			) as photoList
			<![CDATA[
				where rownum > (#{pageNum}-1)*#{amount} and rownum <= #{pageNum}*#{amount}
			]]>
	</select>
	
	
	<select id = "detail" resultType="com.dramac.model.PhotoBoardVO">
		select * from drama_photoboard where bno=#{bno}
	</select>
	
	<update id="modify">
    	update drama_photoboard
    	set title=#{title}, content=#{content} 
    	where bno=#{bno}
    </update>
    
    <delete id="remove">
		delete from drama_photoboard
		where bno=#{bno}
    </delete>
    
	<update id="cntup">
		update drama_photoboard set cnt = cnt+1 where bno=#{bno}
	</update>
	
	<select id="total" resultType="int">
		select count(*) from drama_photoboard
		<if test="keyword != null">
		<choose>
			<when test="type == 'T'.toString()">
			where title like concat('%', #{keyword}, '%')
			</when>
			<when test="type == 'C'.toString()">
			where id like concat('%', #{keyword}, '%')
			</when>
			<when test="type == 'W'.toString()">
			where content like concat('%', #{keyword}, '%')
			</when>
			<otherwise>
			where title like concat('%', #{keyword}, '%')
			OR content like concat('%', #{keyword}, '%')
			</otherwise>
		</choose>
		</if>
	</select>
   
   
</mapper>